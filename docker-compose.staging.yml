version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: t-shop-pod:staging
    restart: always
    ports:
      - '3000:3000'
    env_file:
      - .env.staging
    volumes:
      - media_data:/app/media
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    restart: always
    environment:
      MONGO_INITDB_DATABASE: payload-template-ecommerce
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
  media_data:
